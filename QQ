/*
解密QQ号：现有一串数字 6,3,1,7,5,8,9,2,4，需要解密。解密规则是：首先将第一个数删除，
紧着将第二个数放到这串数的末尾，再将第三个数删除并将第四个数放到这串数的末尾，再将
第五个数的删除。。。直到剩下最后一个数，将最后一个数也删除。按照当时删除的顺序，把
这些数连在一起就是QQ号。 
*/

#include<stdio.h>

int main(void) {
	//该数组用于存储这串数字以及它们的变化情况
	int array[100] = {6, 3, 1, 7, 5, 8, 9, 2, 4};      
	int head = 0;        //head指向第一个元素
	int tail = 9;       //tail指向末尾元素的下一位 
	
	while(head != tail) {
		printf("%d ", array[head++]);
		array[tail++] = array[head++];
	}
	
	return 0;
}
